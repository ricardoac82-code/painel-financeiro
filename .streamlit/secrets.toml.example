# Copie este arquivo para .streamlit/secrets.toml e preencha com o valor real.
# NUNCA commite o secrets.toml de verdade no Git (já está no .gitignore).
# Pegue essa connection string no painel do seu projeto Neon.

NEON_CONNECTION_STRING = "postgresql://usuario:senha@ep-xxxxx.us-east-2.aws.neon.tech/neondb"
